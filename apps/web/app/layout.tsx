import './globals.css';export const metadata={title:'Confido — Strategic Brand Advisory',description:'Brands with direction.'};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
