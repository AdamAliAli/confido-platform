import { Module } from '@nestjs/common'; import { ConfigModule } from '@nestjs/config'; import { AuthModule } from './auth/auth.module'; import { ContentModule } from './content/content.module'; import { PrismaService } from './common/prisma.service'; import { BootstrapService } from './common/bootstrap.service'; import { HealthController } from './health.controller';
@Module({imports:[ConfigModule.forRoot({isGlobal:true}),AuthModule,ContentModule],controllers:[HealthController],providers:[PrismaService,BootstrapService],exports:[PrismaService]}) export class AppModule{}
